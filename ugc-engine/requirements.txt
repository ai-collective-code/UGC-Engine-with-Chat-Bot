# UGC Creator Outreach Engine -- pinned dependencies.
#   pip install -r requirements.txt
#
# The queue builder (build_outreach_queue.py) only needs pandas + openpyxl.
# The send/webhook scripts add requests + flask. The AI negotiator adds anthropic.
# The monitoring dashboard (backend/app.py) reuses flask; the local database
# (lib/local_db.py) is plain sqlite3 from the standard library, no package needed.

pandas>=2.0
openpyxl>=3.1
requests>=2.31
flask>=3.0
anthropic>=0.40
python-dotenv>=1.0
google-generativeai>=0.5.0
# PostgreSQL driver (lib/local_db.py). The -binary wheel needs no system libpq.
psycopg2-binary>=2.9
# Production WSGI server for hosted deploys (e.g. Render). Not used by the local
# `python backend/app.py` entrypoint. Run 1 worker so the in-process scrape-import
# state (lib/app.py _scrape_imported) and SQLite access stay single-process:
#   gunicorn --chdir backend --workers 1 --threads 8 --timeout 120 app:app
gunicorn>=21.2; sys_platform != "win32"
